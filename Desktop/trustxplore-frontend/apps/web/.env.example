# Backend API base URL (no trailing slash).
# Used by config/env.ts → config/api.ts for all API requests.
# Example: https://api.trustxplore.com
NEXT_PUBLIC_API_BASE=

# Alternative name (env.ts checks both)
# NEXT_PUBLIC_API_PATH=
